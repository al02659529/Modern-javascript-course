// const selex = document.querySelector("div.error");

// console.log(selex);

const paras = document.querySelectorAll('p');
const errors = document.querySelectorAll('.error');
// Iterating on an node list
// paras.forEach(para =>{
//     console.log(para);
// });

console.log(errors);

